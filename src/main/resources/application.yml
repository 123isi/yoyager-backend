spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/yoyager
    username: root
    password: yoyager
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
